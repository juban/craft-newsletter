{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "API Key"|t('newsletter'),
    id: 'apiKey',
    name: 'apiKey',
    required: true,
    suggestEnvVars: true,
    value: adapter.apiKey,
    errors: adapter.getErrors('apiKey')
}) }}

{{ forms.autosuggestField({
    label: "Server Prefix"|t('newsletter'),
    id: 'serverPrefix',
    name: 'serverPrefix',
    required: 'true',
    suggestEnvVars: true,
    value: adapter.serverPrefix,
    errors: adapter.getErrors('serverPrefix'),
    instructions: "Mailchimp server prefix (i.e. \"us19\"), in order determine to appropriate host url"|t('newsletter')
}) }}

{{ forms.autosuggestField({
    label: "Audience ID"|t('newsletter'),
    id: 'listId',
    name: 'listId',
    required: 'true',
    suggestEnvVars: true,
    value: adapter.listId,
    errors: adapter.getErrors('listId'),
    instructions: "Audience ID can be found in Mailchimp account in Audience > All contacts > Settings > Audience name and defaults"|t('newsletter')
}) }}
